apiVersion: v1
kind: ConfigMap

metadata:
  name: product-service-config
  namespace: ecommerce

data:
  DB_URL: jdbc:postgresql://postgres-service:5432/productdb
  SPRING_PROFILES_ACTIVE: kubernetes
  SERVER_PORT: "8080"
